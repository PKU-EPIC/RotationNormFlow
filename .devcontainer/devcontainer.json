{
    "name": "PyTorch 3D Dev Container",
    "build": {
        "dockerfile": "../Dockerfile",
        "context": ".."
    },
    "runArgs": [
        "--gpus",
        "all",
        "--ipc=host"
    ],
    "forwardPorts": [],
    "postCreateCommand": "pip install -r requirements.txt torch-tb-profiler",
    "customizations": {
        "vscode": {
            "extensions": [
                "ms-python.python"
            ]
        }
    }
}
